# 2026-09-06 证据链路与控制台发布记录

## 发布内容

- 控制台：<https://elma-gohan.xyz/console/>，同步产品说明、V10 队列/缓存/冷却聚合快照，以及未验收的 80% / 98% 目标。
- 真实网页验收：<https://elma-gohan.xyz/console/app/>。沿用控制台账号密码；点击「开始长沙商圈验收」，使用明确标注的五一商圈固定位置。小程序仍请求实际定位。
- 后端：实时百度 2 次 / 2 秒预算、跨实例调用出口、持久补数队列、严格同店匹配、新深挖消费参考字段；V10 迁移成功。
- 生产旧包已使用百度 AI 搜索，发布保留其环境配置和搜索接口；Brave 实现继续兼容。SafeRegret serving 保持关闭。
- 线上检查发现旧 UTC `timestamp` 与北京时间会话比较的问题，已补发修正缓存保护与任务完成判定，并以 `Asia/Shanghai` 会话测试。

## 可核验结果

- 最终 JAR SHA256：`33a0861672d8c127f9f912d883de253d4f3def71e0c2e717589fec000eef0e60`。
- 运行路径 `/opt/elma-gohan/app.jar`，`elma-gohan.service` 健康；V1–V9 与旧生产迁移逐份核对一致，V10 成功。
- HTTPS `/health` 返回 200；博客首页 200；控制台与 H5 无认证均返回 401。
- 原 `/etc/nginx/elma-console.htpasswd` 和博客 Nginx 配置的 SHA256 均未改变。
- 第一次真实冒烟：麺屋猪一（五一广场店），推荐 2655ms、深挖 1387ms、换一家 88ms。大众点评返回 1 条通过当前同店规则的线索，B 站和小红书为 NO_DATA；返回消费参考与搜索词，换一家沿用原会话快照。不是所有来源都有结果的承诺。
- 相邻商圈位置另发起一次推荐，14:02:15 日志确认 24 家候选补数完成，任务正常退出队列。运行快照以控制台标注时间为准，非实时监控。

## 验证范围

此前本地基础实现后端 259 项：258 通过，1 项真实 POI 基准因无人工样本而跳过。发布增量定向测试 13 项通过；最后时区修复的 5 项协调集成测试再次通过。前端完整 68 项、Python 运营生成器 10 项测试通过，类型检查、H5、小程序构建、OpenAPI 契约校验通过。H5 子目录资源、同源 API 地址和新参考字段静态核对通过。

本机 Windows 提交内存不足导致编译失败。备用服务器全量测试时曾发生资源压力和 SSH/HTTP 暂时超时，该次全量运行被终止，**不记为通过**；停止构建后连接和生产健康恢复。后续采用单 JVM、128MB 堆、400MB cgroup 上限、35% CPU 和运行时限，仅验证增量行为并打包。临时测试库、角色和凭据已清理。生产切换前后均做了健康检查。

尚无 300 家人工标注验收集、50 家深挖对照数据及同批用户前后任务测试。因此不能宣称匹配覆盖率或正确率已达到目标。

## 备份与回退

- 整次发布前：`/opt/elma-gohan/backups/evidence-20260906-v04/`。
- UTC 修复前：`/opt/elma-gohan/backups/evidence-20260906-v04-utc/`。
- 均保存原 JAR、控制台、Nginx snippet 和权限为 0600 的 PostgreSQL 自定义格式备份，位于非公开目录。
- `output/merged-console/release-remote.sh` 在启动健康失败时恢复旧包、控制台和 snippet。数据库不自动回滚，以免覆盖发布后的用户写入；V10 为增量表及列。

## 人工验收

1. 登录控制台，查看产品说明和运营数据的快照时间。
2. 进入网页验收入口，点击长沙商圈验收按钮。
3. 打开「深挖一下」，查看消费参考的出处与日期、公开线索标识及搜索词复制。
4. 使用「换一家」检查体验；空来源允许存在，核实已返回链接是否确为同一分店。

本地小程序构建位于 `dist/build/mp-weixin`，H5 位于 `dist/build/h5`。网页已发布，微信平台的版本上传/审核不属于服务器部署。

## 运营工作台增量更新（2026-09-06 晚间）

按用户确认的范围完善筛选、排序、导出和查询语句查看。默认入口为运营工作台，保留产品演示切换；侧栏组织现有六个运营模块。趋势移除导致线段缺失的 SVG 虚线动画，新增悬浮/键盘数值提示、7/14 天筛选、每日明细及 CSV 导出。表目录支持关键词、分组、行数排序、目录导出和查询弹窗，补充 V10 三张表说明。

表名被递归转成驼峰键名曾导致行数查找失败。本次生成器保留 `tableRows` 的真实表名，页面兼容旧键名；零行显示 `0 行`，缺少统计显示 `快照未包含行数`。新生产快照时间为 `2026-09-06T19:03:51.285461+08:00`，包含 18 张表的行数。数据仍为只读聚合快照，页面不执行 SQL，也不编辑数据库记录。

- 前端 72 项测试、类型检查、Python 生成器 11 项测试通过；生成器检查 `DASHBOARD_CHECK_OK queries=26`。
- 生产快照合页运行检查通过：默认运营页、趋势存在、18 张表统计、无脚本运行错误。
- 仅原子替换 `/var/www/elma-console/index.html`；备份 `/opt/elma-gohan/backups/console-admin-20260906-1907/index.html`。
- 新 HTML SHA256：`5c4b0d2b45c5fbe4a303d344eb7f16a2f4b7faa45bd2164351eb73fd4ef3a70d`，远端文件校验一致。
- 发布后应用服务 active，API 健康与博客 200；控制台及 H5 未登录访问 401。本地没有保存的登录凭据，未自动验证登录后的 HTTP 响应；页面视觉与真实浏览器悬浮体验交由用户人工验收。
